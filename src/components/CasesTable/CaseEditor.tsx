export default function CaseEditor({ caseId }: { caseId: string }) {
  return (
    <div>
      <h2 className="text-lg font-semibold">Edit Case: {caseId}</h2>
      {/* Add your case editing form or components here */}
    </div>
  );
}
